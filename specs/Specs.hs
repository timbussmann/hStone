-- automatic spec test discovery (see http://hspec.github.io/hspec-discover.html)
-- use runhaskell -ispecs:src specs/specs.hs from the root directory to run all specs
{-# OPTIONS_GHC -F -pgmF hspec-discover #-}
